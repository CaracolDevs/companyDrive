<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/styles/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>
<body>
    <header>
        <div>
            <img class="logo" src="/images/logo.jpg" alt="Logo">
        </div>
    </header>
    
    <div class="displayContainer">
        <ul class="display-ul">
            <% for(var i = 0; i < result.length; i++) { %>
                <li ><span class="icon-caracol"></span><a class="a-list" href="/values/delete/<%= result[i].ProcesoId %>">Borrar</a>
                    <span class="tag" 
                    style="background-color:
                <% if (result[i].Result < 20) { %> blueviolet <% } %>
                <% if (result[i].Result >= 20 && result[i].Result < 70) { %> green <% } %>
                <% if (result[i].Result >= 70 && result[i].Result < 200) { %> yellow <% } %>
                <% if (result[i].Result >= 200 && result[i].Result < 400) { %> orange <% } %>
                <% if (result[i].Result > 400) { %> red <% } %>
                "
                    ></span>
                    <span> 
                    | <%= result[i].Proceso %> | <%= result[i].Puestos %> | <%= result[i].Tarea %> | <%= result[i].Actividades %>
                    | <%= result[i].Rutinario %> | <%= result[i].TipoPeligro %> | <%= result[i].Peligro %> | <%= result[i].Riesgo_Consecuencia %>
                    | <%= result[i].Controles %> | <%= result[i].ControlesValores %> | <%= result[i].Consecuencias %> | <%= result[i].Exposicion %>
                    | <%= result[i].Riesgo %> | <%= result[i].Result %> | <%= result[i].ResultText %>
                    
                <span/>
                </li>
            <% } %>
        </ul>
    </div>

    <a class="custom-btn" href="javascript:history.back()">Volver</a>
</body>
</html>